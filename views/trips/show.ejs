<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <% include ../partials/head %>
  </head>
  <body>
    <div class="container">


    <nav>
      <ul>
        <li><a href="/trips/<%= trip.id %>/edit">add a description</a></li>
      </ul>

    </nav>


    <ul>
      <li><%= trip.username %></li>
      <li>From: <%= trip.origin %></li>
      <li>To: <%= trip.destination %></li>
    </ul>
     <% let time = trip.updatedAt - trip.createdAt %>
     <% time = Math.floor(time/1000);%>
     <h2>Your Time: <%= time %> seconds</h2>
     <h3><%=trip.yourTime %></h3>
     <p><%= trip.description %></p>

     </div>

  </body>
</html>
